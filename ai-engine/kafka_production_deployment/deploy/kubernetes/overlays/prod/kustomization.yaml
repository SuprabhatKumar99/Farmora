apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namePrefix: prod-
patches:
  - target:
      kind: Deployment
      name: ai-engine
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
